{
  "config": {
    "step": {
      "user": {
        "title": "Polygonal Zones Configuration",
        "description": "Select GeoJSON file location",
        "data": {
          "zone_urls": "URLs of GeoJSON files",
          "prioritize_zone_files": "Prioritize order of zone files",
          "registered_entities": "Entities",
          "download_zones": "Download the GeoJSON files"
        },
        "data_description": {
          "zone_urls": "Enter the URLs of the GeoJSON files that contain the zones you want to track. This supports both websites and files in the /config directory.",
          "prioritize_zone_files": "If you want to prioritize the order of the zone files, enable this option. This means that if a tracker is in multiple zones it will only consider those with the lowest priority.",
          "download_zones": "Use a local GeoJSON file to store the zones in. This will load the above defined files into a single file. The entities will only use this single file to retrieve the zones from. If no GeoJSON files are defined we will create an empty GeoJSON file.",
          "registered_entities": "Select the entities that you want to track in the zones."
        }
      }
    },
    "error": {
      "invalid_url": "One or more URLs or file paths are invalid",
      "unreachable_url": "One or more URLs cannot be reached",
      "no_entities": "No entities selected",
      "invalid_path": "The file could not be found",
      "download_or_no_zones": "Download zones needs to be enabled or links to GeoJSON files need to be defined"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Polygonal Zones Options",
        "description": "Select GeoJSON file location",
        "data": {
          "zone_urls": "URLs of GeoJSON files",
          "prioritize_zone_files": "Prioritize order of zone files"
        },
        "data_description": {
          "zone_urls": "Enter the URLs of the GeoJSON files that contain the zones you want to track. This supports both websites and files in the /config directory.",
          "prioritize_zone_files": "If you want to prioritize the order of the zone files, enable this option. This means that if a tracker is in multiple zones it will only consider those with the lowest priority."
        }
      }
    },
    "error": {
      "invalid_url": "One or more URLs or file paths are invalid"
    }
  },
  "services": {
    "reload_zones": {
      "name": "Reload Zones",
      "description": "Reload the zones stored in the GeoJSON files. This will retrieve them from the stored location and update the entities internal cache of the zones."
    },
    "replace_all_zones": {
      "name": "Replace All Zones",
      "description": "Replace all existing zones with new ones defined in GeoJSON format",
      "fields": {
        "zone": {
          "name": "GeoJSON Data",
          "description": "The GeoJSON representation of all zones to replace the existing ones"
        }
      }
    },
    "add_new_zone": {
      "name": "Add Zone",
      "description": "Add a new zone to the existing zones",
      "fields": {
        "zone": {
          "name": "Zone Data",
          "description": "The GeoJSON representation of the new zone to add"
        }
      }
    },
    "edit_zone": {
      "name": "Edit Zone",
      "description": "Modify an existing zone's boundaries",
      "fields": {
        "zone_name": {
          "name": "Zone Name",
          "description": "The name of the zone to edit"
        },
        "zone": {
          "name": "Zone Data",
          "description": "The GeoJSON representation of the modified zone"
        }
      }
    },
    "delete_zone": {
      "name": "Delete Zone",
      "description": "Remove a zone from the system",
      "fields": {
        "zone_name": {
          "name": "Zone Name",
          "description": "The name of the zone to delete"
        }
      }
    }
  }
}
